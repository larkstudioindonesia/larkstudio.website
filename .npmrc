# Reproducibility. Every `npm install <pkg>` writes an exact version,
# never a caret range. Combined with a committed package-lock.json and
# `npm ci` in CI, this is what makes the build reproducible in ten
# years — the manifest pins alone do not, since transitive
# dependencies would still float.
save-exact=true
engine-strict=true
